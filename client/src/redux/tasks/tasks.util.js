// export const addTaskToList = (tasks, taskToAdd) => {
//     return [...tasks, { ...taskToAdd }]
// };

// export const deleteTaskFromList = (tasks, taskToDelete) => {
//     const existingTask = tasks.find(task => task.id === taskToDelete.id);

//     if (existingTask) {
//         return tasks.filter(task => task.id !== taskToDelete.id);
//     };

//     return tasks;

// };


